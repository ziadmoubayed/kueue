---
apiVersion: v1
kind: ConfigMap
metadata:
  name: kueueviz-frontend-env
  namespace: system
data:
  env.js: |
    window.env = {
      REACT_APP_WEBSOCKET_URL: "wss://backend.kueueviz.local",
      VITE_WEBSOCKET_URL: "wss://backend.kueueviz.local"
    };
